import AppShell from '../../../components/AppShell';import {prisma} from '../../../lib/prisma';import {getCurrentUser} from '../../../lib/session';import {redirect} from 'next/navigation';
export const dynamic='force-dynamic';
export default async function NewEquipment(){const u=await getCurrentUser();if(!u||u.role.name!=='Admin')redirect('/equipment');const locations=await prisma.location.findMany({orderBy:{name:'asc'}});const allowed=new Set((u.accessibleLocations||[]).filter(a=>a.roleName==='Admin').map(a=>a.id));const visibleLocations=locations.filter(l=>allowed.has(l.id));return <AppShell title="Add Equipment" active="Equipment"><section className="card formCard"><h2>New Equipment</h2><p className="muted">Create the machine first. Parts and service intervals can be added immediately afterward.</p><form action="/api/equipment" method="post" encType="multipart/form-data"><div className="formgrid"><div className="field"><label>Unit / Equipment ID</label><input name="code" placeholder="Example: FL-4" required/></div><div className="field"><label>Current Hours</label><input name="currentHours" type="number" min="0" step="0.1" defaultValue="0"/></div><div className="field"><label><input name="usesMileage" type="checkbox" value="1"/> Track Mileage</label><div className="muted">Enable only for vehicles or road equipment.</div></div><div className="field"><label>Current Mileage <span className="muted">(optional)</span></label><input name="currentMileage" type="number" min="0" step="1" placeholder="Hidden everywhere unless Track Mileage is enabled"/></div><div className="field full"><label>Description</label><input name="displayName" placeholder="Example: Sennebogen Material Handler"/></div><div className="field"><label>Location</label><select name="locationId">{visibleLocations.map(l=><option value={l.id} key={l.id}>{l.name}</option>)}</select></div><div className="field"><label>Status</label><select name="status"><option value="ACTIVE">Active</option><option value="OUT_OF_SERVICE">Out of Service</option><option value="RETIRED">Retired</option><option value="SOLD">Sold</option><option value="ARCHIVED">Archived</option></select></div><div className="field full"><label>Equipment Photo</label><input name="photo" type="file" accept="image/*"/></div></div><div className="formFooter"><a href="/equipment" className="btn">Cancel</a><button className="btn primary">Create Equipment</button></div></form></section></AppShell>}

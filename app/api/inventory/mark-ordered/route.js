import {NextResponse} from 'next/server';import {requireApiUser} from '../../../../lib/apiAuth';import {markOrdered} from '../../../../lib/inventoryService';
export async function POST(req){try{const a=await requireApiUser(['Admin','Scale House']);if(a.error)return NextResponse.json({error:a.error},{status:a.status});const b=await req.json();if(Number(b.locationId)!==Number(a.user.locationId))return NextResponse.json({error:'Location access denied'},{status:403});const result=await markOrdered({...b,userId:a.user.id});return NextResponse.json({ok:true,result})}catch(e){return NextResponse.json({error:e.message},{status:400})}}

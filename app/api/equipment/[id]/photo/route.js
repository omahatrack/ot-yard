import {NextResponse} from 'next/server';import {prisma} from '../../../../../lib/prisma';import {sql} from '../../../../../lib/db';import {requireApiUser} from '../../../../../lib/apiAuth';import {getCurrentUser,userCanAccessLocation} from '../../../../../lib/session';import {redirectTo} from '../../../../../lib/http';import {uploadStorageObject,downloadStorageObject,deleteStorageObject,getSupabaseStorageConfig} from '../../../../../lib/supabaseStorage';import {writeAudit} from '../../../../../lib/audit';
const ALLOWED=new Set(['image/jpeg','image/png','image/webp']);
function validMagic(buf,type){if(type==='image/jpeg')return buf.length>3&&buf[0]===0xff&&buf[1]===0xd8&&buf[2]===0xff;if(type==='image/png')return buf.length>8&&buf.subarray(0,8).equals(Buffer.from([0x89,0x50,0x4e,0x47,0x0d,0x0a,0x1a,0x0a]));if(type==='image/webp')return buf.length>12&&buf.subarray(0,4).toString()==='RIFF'&&buf.subarray(8,12).toString()==='WEBP';return false}
function photoPath(id,name){const safe=String(name||'photo').replace(/[^a-zA-Z0-9._-]+/g,'_').slice(-140);return `equipment/${id}/photo/${Date.now()}-${safe}`}
export async function GET(_req,{params}){const {id:rawId}=await params,id=Number(rawId),user=await getCurrentUser();if(!user)return new Response('Unauthorized',{status:401});const e=await prisma.equipment.findUnique({where:{id}});if(!e||!userCanAccessLocation(user,e.locationId))return new Response('Not found',{status:404});const rows=await sql('SELECT mimeType,fileName,data,storageProvider,storageBucket,storagePath,updatedAt FROM EquipmentPhoto WHERE equipmentId=? LIMIT 1',[id]);if(!rows.length)return new Response(null,{status:302,headers:{Location:'/img/sc7.jpg','Cache-Control':'no-store'}});const r=rows[0];let data;if(r.storageProvider==='supabase'&&r.storagePath)data=await downloadStorageObject(r.storageBucket,r.storagePath);else data=Buffer.from(r.data||[]);return new Response(data,{status:200,headers:{'Content-Type':r.mimeType,'Content-Length':String(data.length),'Cache-Control':'private, max-age=60'}})}
export async function POST(req,{params}){const a=await requireApiUser(['Admin']);if(a.error)return NextResponse.json({error:a.error},{status:a.status});const {id:rawId}=await params,id=Number(rawId),e=await prisma.equipment.findUnique({where:{id}});if(!e)return NextResponse.json({error:'Not found'},{status:404});if(!userCanAccessLocation(a.user,e.locationId,['Admin']))return NextResponse.json({error:'Location access denied'},{status:403});const f=await req.formData(),photo=f.get('photo');if(!photo||typeof photo.arrayBuffer!=='function')return NextResponse.json({error:'Choose an image first.'},{status:400});if(!ALLOWED.has(photo.type))return NextResponse.json({error:'Use JPG, PNG or WEBP.'},{status:400});if(photo.size>8*1024*1024)return NextResponse.json({error:'Image must be 8 MB or smaller.'},{status:400});if(!getSupabaseStorageConfig().configured)return NextResponse.json({error:'Supabase Storage is not configured yet.'},{status:503});const data=Buffer.from(await photo.arrayBuffer());if(!validMagic(data,photo.type))return NextResponse.json({error:'The uploaded file does not match its image format.'},{status:400});const old=(await sql('SELECT storageProvider,storageBucket,storagePath FROM EquipmentPhoto WHERE equipmentId=? LIMIT 1',[id]))[0];const path=photoPath(id,photo.name),stored=await uploadStorageObject(path,data,photo.type);await sql(`INSERT INTO EquipmentPhoto(equipmentId,mimeType,fileName,data,storageProvider,storageBucket,storagePath) VALUES (?,?,?,NULL,'supabase',?,?) ON DUPLICATE KEY UPDATE mimeType=VALUES(mimeType),fileName=VALUES(fileName),data=NULL,storageProvider='supabase',storageBucket=VALUES(storageBucket),storagePath=VALUES(storagePath),updatedAt=CURRENT_TIMESTAMP(3)`,[id,photo.type,String(photo.name||'equipment-photo'),stored.bucket,stored.path]);if(old?.storageProvider==='supabase'&&old.storagePath)await deleteStorageObject(old.storageBucket,old.storagePath);await prisma.equipment.update({where:{id},data:{imagePath:`/api/equipment/${id}/photo`}});await writeAudit({userId:a.user.id,locationId:e.locationId,entityType:'EquipmentPhoto',entityId:id,action:'UPDATE',summary:`Updated photo for ${e.code}`});return redirectTo(`/equipment/${id}`)}
export async function DELETE(_req,{params}){const a=await requireApiUser(['Admin']);if(a.error)return NextResponse.json({error:a.error},{status:a.status});const {id:rawId}=await params,id=Number(rawId),e=await prisma.equipment.findUnique({where:{id}});if(!e)return NextResponse.json({error:'Not found'},{status:404});if(!userCanAccessLocation(a.user,e.locationId,['Admin']))return NextResponse.json({error:'Location access denied'},{status:403});const old=(await sql('SELECT storageProvider,storageBucket,storagePath FROM EquipmentPhoto WHERE equipmentId=? LIMIT 1',[id]))[0];if(old?.storageProvider==='supabase'&&old.storagePath)await deleteStorageObject(old.storageBucket,old.storagePath);await sql('DELETE FROM EquipmentPhoto WHERE equipmentId=?',[id]);await prisma.equipment.update({where:{id},data:{imagePath:null}});await writeAudit({userId:a.user.id,locationId:e.locationId,entityType:'EquipmentPhoto',entityId:id,action:'DELETE',summary:`Removed photo for ${e.code}`});return NextResponse.json({ok:true})}

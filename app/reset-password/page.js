export const dynamic='force-dynamic';
export default function ResetPassword({searchParams}){const token=String(searchParams?.token||'');const status=searchParams?.status;return <div className="login"><section className="loginVisual"><img src="/img/logo-dark.png" alt="OTE"/><h1>Create a new password.</h1><p>For security, reset links are single-use.</p></section><section className="loginPanel"><img src="/img/logo-light.png" alt="OTE"/><h2>Set New Password</h2>{status==='done'?<><div className="successBox">Password updated successfully.</div><a className="btn primary" href="/" style={{textAlign:'center',marginTop:16}}>Go to Login</a></>:token?<form action="/api/auth/reset-password" method="post"><input type="hidden" name="token" value={token}/><div className="field"><label>New Password</label><input name="password" type="password" minLength="8" required autoComplete="new-password"/></div><div className="field" style={{marginTop:14}}><label>Confirm Password</label><input name="confirmPassword" type="password" minLength="8" required autoComplete="new-password"/></div><button className="btn primary" style={{width:'100%',marginTop:18}}>Update Password</button></form>:<div className="error">This reset link is missing or invalid.</div>}<a className="textLink loginHelp" href="/">← Back to login</a></section></div>}

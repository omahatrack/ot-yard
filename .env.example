SESSION_SECRET="generate-a-long-random-secret-in-godaddy"
# DB_HOST, DB_PORT, DB_NAME, DB_USER and DB_PASSWORD are injected automatically by GoDaddy Hosted Database.

# Optional outbound email for password reset
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
